[project]
name = "astrbot-plugin-activitywatch-peek"
version = "v1.0.0"
description = "利用 ActivityWatch 拉取你当前的焦点进程并推送，与 aw-peek 配合使用"
requires-python = ">=3.12"
dependencies = [
    "astrbot>=4.12.3",
    "httpx>=0.27.0",
]
